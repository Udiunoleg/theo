# 2026-02-12

- Decisione Teo: migrare da Feedly a FreshRSS per avere controllo API e workflow read/read-later integrabile con T.H.E.O.
- FreshRSS bootstrap completato in workspace (`freshrss/FreshRSS`) con database SQLite, API abilitata, utente `teo` creato e feed importati (Geopop, Multiplayer, Sky TG24).
- Prima sincronizzazione eseguita: 3 feed aggiornati, 160 articoli caricati.
- Stato pending per domattina: Teo non è al Mac; riprendere setup FreshRSS dal punto "accesso iPhone via tunnel + cambio password + test API read/read-later + digest 10:00".
- Debug accesso FreshRSS: errore `ERR_TOO_MANY_REDIRECTS` con URL che si allungava (`/i/p/p/p...`) risolto aggiornando `freshrss/FreshRSS/data/config.php` → `base_url` da `http://127.0.0.1:18990` a `http://127.0.0.1:18990/p`.
- Per accesso da iPhone via LAN durante tunnel SSH, necessario bind pubblico locale della porta FreshRSS: usare `-g` e `-L 0.0.0.0:18990:127.0.0.1:18990` (oltre alle porte OpenClaw già in uso).
- FreshRSS messo persistente come servizio user systemd (`freshrss-php.service`) e corretto bind di sicurezza: ascolto solo su IP Tailscale `100.90.22.124:18990` (non su IP pubblico).
- Nuove preferenze digest Geopop concordate con Teo: formato dettagliato con stile leggibile (emoji + bold/italic), senza sezione "Perché conta"; 1 messaggio Telegram per articolo (mai spezzare titolo/descrizione su messaggi diversi); includere sempre Titolo + descrizione riassuntiva ricca + link.
- Tag obbligatorio per ogni articolo nel digest: `🟢 Leggi tutto` oppure `🟡 Riassunto basta`; aggiungere tag video quando presente (`🎥 Video presente`).
- Workflow operativo richiesto da Teo dopo invio digest: ogni articolo inviato va marcato subito come `letto` su FreshRSS; se l’articolo contiene video va marcato anche come `preferito` (watch later). In caso di dubbio detection video, prevale segnalazione manuale di Teo.
- Test feedback inline Telegram riuscito: bottoni sotto articolo funzionanti. Teo ha cliccato `feedback_stem_high` sul contenuto STEM (interesse alto), da usare per affinare tag e priorità nei prossimi digest.
- Preferenza feedback UI aggiornata: nei bottoni inline usare etichette coerenti ai tag digest (`🟢 Leggi tutto` / `🟡 Riassunto basta`) invece di "interessante / meno di questo".
- Feedback ricevuti: `feedback_iguane_low` e `feedback_mascotte_low` (curiosità leggere/mascotte da tenere su priorità bassa, tag giallo).
- Decisione operativa confermata da Teo: usare stabilmente bottoni inline sotto ogni articolo del digest (`🟢 Leggi tutto` / `🟡 Riassunto basta`) e gestire feedback asincroni senza bloccare il flusso.
- Orchestrazione modelli formalizzata (MOP): Flash default, Pro per output contenutistici complessi, Codex riservato a task tecnici/operativi; guardrail principale su quota settimanale Codex (warning <40%, conservativa <25%, critici-only <15%).
- `"/mop"` adottato come comando operativo per mostrare pannello bottoni di switch modello (`⚡ Flash`, `🧠 Pro`, `🛠️ Codex`, `🔁 MOP auto`).
- Fix conferme: Teo ha richiesto ack esplicito post-azione; da ora rispondere sempre con conferma tipo `Fatto ✅` quando un comando/azione viene applicato.
- Nota affidabilità: clic bottone `model_mop_auto` da solo non applica cambio modello; applicare realmente il model switch via stato sessione e confermare modello attivo.
- Date personali importanti condivise da Teo: anniversario di coppia con Jessica il 27 marzo 2013; data matrimonio 27 marzo 2021.
- "La Prima di T.H.E.O": Teo ha approvato come formato definitivo il PDF in stile newspaper (impaginazione giornale) usata nel file `la-prima-theo-2026-02-11.html`; da usare sempre come template grafico, mantenendo contenuti estesi della versione aggiornata.
- Nuova regola esplicita Geopop on-demand: quando Teo chiede "dammi un articolo Geopop", prendere sempre l'articolo più vecchio tra i non letti su FreshRSS, inviarlo nel formato digest concordato e poi marcarlo come letto.
- Feedback Geopop registrato: `feedback_whatsapp_high` (interesse alto su tema blocchi app social/controllo informazione digitale).
